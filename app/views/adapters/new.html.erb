<% page_title "#{action_name.capitalize} Adapter" %>

<div class="space-y-6">
  <div class="sm:flex sm:items-center sm:justify-between">
    <h1 class="text-2xl font-bold text-gray-900"><%= action_name.capitalize %> Adapter</h1>
  </div>

  <div class="bg-white shadow rounded-lg p-6">
    <%= render "form", adapter: @adapter %>
  </div>
</div>
